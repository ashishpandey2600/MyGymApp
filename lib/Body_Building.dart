import 'package:flutter/material.dart';

class Body_Building extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Body Building")),
      body: Container(),
    );
  }
}
