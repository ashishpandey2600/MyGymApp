import 'package:flutter/material.dart';

class Fee_Details extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Fee Details")),
      body: Container(),
    );
  }
}
