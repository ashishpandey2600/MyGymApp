import 'package:flutter/material.dart';

class Strength extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Strength")),
      body: Container(),
    );
  }
}
