import 'package:flutter/material.dart';

class Steam_Bath extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Steam Bath")),
      body: Container(),
    );
  }
}
