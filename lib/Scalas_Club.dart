import 'package:flutter/material.dart';

class Scalas_Club extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Scalas Club")),
      body: Container(),
    );
  }
}
